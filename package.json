{
  "name": "ants",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --watch packages/api/src/index.ts",
    "dev:all": "concurrently -n api,ui -c blue,green 'bun run --watch packages/api/src/index.ts' 'cd packages/ui && bunx vite --host --port 3001'",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "db:migrate": "drizzle-kit migrate",
    "db:generate": "drizzle-kit generate",
    "setup:db": "bun run scripts/setup-first-user.ts",
    "spec:bundle": "bun run scripts/bundle-spec.ts",
    "spec:validate": "bun run scripts/bundle-spec.ts && npx @redocly/cli lint openapi/bundled.yaml",
    "spec:types": "bun run scripts/bundle-spec.ts && npx openapi-typescript openapi/bundled.yaml -o packages/core/src/types/api.d.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@redocly/cli": "2.32.0",
    "@types/bun": "1.3.13",
    "concurrently": "^10.0.3",
    "drizzle-kit": "^0.31.0",
    "openapi-typescript": "7.13.0",
    "typescript": "6.0.3",
    "yaml": "2.7.1"
  }
}
